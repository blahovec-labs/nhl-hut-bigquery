[project]
name = "nhl-hut-bigquery"
version = "0.1.0"
description = "EA NHL HUT player ratings → BigQuery snapshots + cross-source resolver to nhl-bigquery"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Jason Blahovec" }]
requires-python = ">=3.11"
keywords = ["nhl", "hockey", "ea-sports", "hut", "bigquery"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "requests>=2.31,<3.0",
    "pandas>=2.0,<3.0",
    "google-cloud-bigquery>=3.20,<4.0",
    "pyarrow>=15.0,<19.0",
    "db-dtypes>=1.0,<2.0",
    "beautifulsoup4>=4.12,<5.0",
    "Unidecode>=1.3,<2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6.0",
    "pyright>=1.1.380",
    "build>=1.2.0",
    "responses>=0.25",
]

[project.scripts]
nhl-hut-bigquery = "nhl_hut_bigquery.cli:main"

[project.urls]
Homepage = "https://github.com/blahovec-labs/nhl-hut-bigquery"
Issues = "https://github.com/blahovec-labs/nhl-hut-bigquery/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nhl_hut_bigquery"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "W"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "standard"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "network: tests that hit the live HUT site; skip in CI",
]
